#lang racket

;Answer: In previous eval/apply construction, I assume that the language is
;text already convert to a data structure like AST. And then, the the data
;structure can differ by redefine a constructor and selector. to met new
;syntax. And the registed eval procedure stay the same.
